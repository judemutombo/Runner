
#pragma once 

enum Direction{
    LEFT,
    RIGHT,
    BACK,
    FRONT
};

enum Element{
    BLOCK,
    ROAD,
    START,
    TREES,
    VOID
};

enum Orientation{
    O_LEFT,
    O_RIGHT,
    O_UP,
    O_DOWN
};

enum GearBox{
    REVERSE,
    DRIVE,
    PARK
};